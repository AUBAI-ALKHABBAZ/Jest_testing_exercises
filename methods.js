const { beforeAll, afterAll } = require("jest-circus");

/*
befoteAll(fn,timeout)
beforeEach(fn,timeout)

*/
beforeAll(() => {
    //Connect to Database
    //Empty testing Table 
    //Add Dummy Data for testing
    //prepare Everything Before Testing
});
afterAll(()=>{
    //clear database
    //clear cache

});